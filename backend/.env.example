# Database
DATABASE_URL=postgresql+psycopg2://nexsolve:nexsolve_password@localhost:5432/nexsolve

# JWT
JWT_SECRET_KEY=change_this_to_a_long_random_secret
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# SMTP (optional for POC)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@nexsolve.local

# Storage
UPLOADS_DIR=./uploads
